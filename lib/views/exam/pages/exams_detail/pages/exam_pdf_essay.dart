import 'package:flutter/material.dart';

class ExamPdfEssay extends StatelessWidget {
  final String name;
  final String public;

  const ExamPdfEssay({
    required this.name,
    required this.public,
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
